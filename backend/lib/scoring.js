// Points: correct result = 1pt, correct prop = 2pts, max 7pts per match

const POINTS = {
  CORRECT_RESULT: 1,
  CORRECT_PROP: 2,
};

function scoreSubmission(prediction, actualResult, actualProps) {
  // TODO: Phase 5 — compare prediction vs actuals, return points breakdown
  let total = 0;
  return { total, breakdown: {} };
}

module.exports = { POINTS, scoreSubmission };
